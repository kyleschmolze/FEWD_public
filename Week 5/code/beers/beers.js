// This is a javascript comment. It starts with "//". Anything after the "//" will be ignored.

/* 
   This is a javascript multiline comment, 
   you'll notice that it uses the same syntax as a CSS comment! 
*/

/*
  This is your first javascript file! It has already been linked to from our HTML file 
  (much like linking a CSS file, just with different syntax). Any javascript code you 
  put in here will be run immediately when the page loads. So just refresh the page 
  to run new code!
*/


